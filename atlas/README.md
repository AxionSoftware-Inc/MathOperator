# Modular Atlas authoring

The canonical runtime loader accepts a directory of `.json` modules. The current files demonstrate the versioned modular format; richer identity, relation, regularity, evidence, and provenance fields are added without changing the loader boundary.

```bash
./build/opforge atlas validate atlas
./build/opforge atlas stats atlas
./build/opforge atlas graph atlas
```
